# Frontend configuration
# The frontend talks only to the backend API and holds no Azure credentials.
# All Azure access is server-side via Managed Identity / Azure AD RBAC.

# Base URL of the backend API (default: http://localhost:5000)
VITE_API_URL=http://localhost:5000
